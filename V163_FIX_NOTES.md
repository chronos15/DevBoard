# TaskBoard V163 — nomenclatura SOMENTE LEITURA

## Ajuste visual

- Substituído o texto visual `READ ONLY` por `SOMENTE LEITURA` em toda a interface de permissões e nos avisos de módulos bloqueados para escrita.
- O seletor visual do modo também passa a exibir `Somente leitura`.
- Nenhuma chave interna, enum, migration, RPC, regra de acesso ou estrutura de banco foi alterada.
- O comportamento implementado na V161 permanece exatamente o mesmo.

## Arquivos alterados

- `components/config/config-view.tsx`
- `components/project-detail/project-follow-up.tsx`
- `components/analysis/analysis-view.tsx`
- `components/requests/request-detail.tsx`
- `lib/app-version.ts`
- `next.config.mjs`
