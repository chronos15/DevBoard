# TaskBoard V166 — destaque de execução no modo escuro

## Projetos > Atividades

- A faixa **Executando** do card de atividade agora usa texto laranja e fundo laranja suave também no modo escuro.
- O indicador pulsante de execução usa a mesma identidade laranja para manter contraste.
- O seletor de status da subatividade em **Em execução** recebe fundo laranja suave e texto laranja, sem alterar as cores globais dos outros módulos.
- Subatividade em execução ganhou card mais arredondado (`rounded-2xl`), borda laranja discreta e fundo de execução levemente destacado.
- Nenhuma regra de status, timer ou banco foi alterada.
